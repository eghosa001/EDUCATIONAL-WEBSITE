/**
 * LEGACY CURRICULUM SQL GENERATOR — DISABLED
 *
 * The previous implementation synthesized lessons, questions and flashcards
 * from topic labels using generic templates. Those templates are unsuitable for
 * production learning content because they are not grounded in the actual
 * lesson or curriculum objectives.
 *
 * Generate/import production content only through the maintained validated
 * curriculum content pipeline.
 */

console.error(
  'generate-content-sql.js is disabled: legacy generic content generation is unsafe for production.'
);
process.exit(1);
