# Production readiness scope

This release hardening applies to the public web application, admin application, backend API, deployed Supabase Edge Functions, and their production deployment/smoke gates.

The Flutter mobile application remains an independent deliverable and is validated by its own workflow.
