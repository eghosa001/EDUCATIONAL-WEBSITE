// Backwards-compatible DI surface. The canonical provider graph lives in
// container.dart; keeping one graph avoids duplicate Provider instances and
// divergent import paths.
export 'container.dart';
