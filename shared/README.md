# Shared Types & Configuration

## Structure
```
shared/
├── types/                  # Shared TypeScript/Dart types
│   ├── api/                # API request/response types
│   ├── models/             # Domain models (User, Course, Lesson, etc.)
│   ├── database/           # Database entity types
│   └── events/             # Event types for messaging
├── config/                 # Shared configuration
├── constants/              # Shared constants (enums, status codes)
├── utils/                  # Shared utilities
��── schemas/                # Validation schemas (Zod, JSON Schema)
```