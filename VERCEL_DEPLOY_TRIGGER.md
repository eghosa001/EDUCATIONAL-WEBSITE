# Vercel deployment trigger

This file intentionally contains no application logic. It exists to trigger a fresh Git-based deployment while Vercel project configuration is being corrected.

Do not add secrets or environment variables to this file.
