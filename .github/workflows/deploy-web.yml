name: Legacy Vercel Website Deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy deployment disabled
        run: |
          echo 'The production frontend now deploys through .github/workflows/deploy-pages.yml.'
          echo 'This workflow is retained only as a manual rollback reference.'
