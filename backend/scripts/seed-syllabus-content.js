/**
 * LEGACY SYLLABUS CONTENT SEEDER — DISABLED
 *
 * This seeder generated questions and lesson material from broad subject/topic
 * templates rather than the exact lesson text. It is intentionally blocked so
 * generic questions cannot be reintroduced into production.
 *
 * Use the validated curriculum content pipeline and lesson-grounded practice
 * generation instead.
 */

console.error(
  'seed-syllabus-content.js is disabled: legacy template generation is not production-safe.'
);
process.exit(1);
